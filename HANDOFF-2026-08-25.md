# Session handoff — Project Zara, 2026-08-25

For a fresh agent picking this up. Everything substantive is already written to files in
the repo; this document is a map plus the things that only exist in conversation.

**Do not re-derive anything below. It cost live API calls and several wrong turns.**

---

## 1. Read these first, in this order

| Path | What it is |
|---|---|
| `CLAUDE.md` | Project rules, the 10 Compasses, environment gotchas |
| `~/.claude/plans/read-agent-transfer-ag-to-c-10-md-antigr-mossy-locket.md` | **The main artifact.** 7 product rulings, 24 catalogued defects (D1–D24) with file:line, 3 root causes, measured facts, open design questions Q1–Q6 |
| `reference/stress_findings.md` | Defects found by running real prospects (F1–F3), each with root cause and fix |
| `perplexity_responses/02_llm_providers_structured_output.md` | Provider research **+ my verification notes appended at the bottom** |
| `perplexity_responses/03_signal_discovery_apis.md` | Signal-source research **+ verification notes** |
| `agent_transfer/AG_to_C_10.md` | The original problem statement this session responded to |
| `HANDOFF.md` | Prior session handoff — **two entries are stale, see §5** |

Raw run data: `reference/stress_log.jsonl` (machine-readable) and `reference/stress_log.md` (table).

---

## 2. Working mode (the human stated this explicitly)

- **Claude is the brain.** Do the analysis, decide, and work directly in the codebase.
- **Antigravity is an executioner only.** Do not write long `C_to_AG_*.md` review documents
  as the default output — the three-party round-trip burns context restating known things.
- **Do not spawn subagents** unless asked.
- Present decisions as labelled A/B/C options with a recommendation, via `AskUserQuestion`.
  Never open-ended questions.
- Perplexity has no MCP access. Research routes through the human:
  write `perplexity_prompts/NN_*.md`, they paste it, reply lands in `perplexity_responses/`.
- **Perplexity hallucinates confidently on this project.** Verify every number against a
  primary source or our own dashboards before acting. See §5.

Persisted to memory already: `claude-is-brain-minimize-ag-transfer`, `zara-product-rulings`.

---

## 3. Current state

**15 files modified, nothing committed.** The human was offered a commit and invoked handoff
instead — so **offer the commit again before starting new work.** Modified:

```
zara/{ranker,drafter,verifier,s2,models,orchestrator}.py
zara/utils/provider.py  zara/fetchers/apify.py
value_prop.yaml  tests/test_s2.py
scripts/{create_snapshot,record_mock}.py   (pre-existing AG changes, not mine)
tests/fixtures/* (2 re-recorded + apify fixture)
```

New untracked: `scripts/stress_run.py`, `reference/stress_findings.md`,
`reference/stress_set.csv`, `perplexity_prompts/04_*.md`, `perplexity_responses/0{2,3}_*.md`.

**Tests: 15 pass, twice consecutively.** `tests/test_discovery.py::test_known_companies` is
flaky — it makes live network calls to real ATS APIs and passes in isolation. Not a regression.

---

## 4. What was done this session

1. **Diagnosis** — reviewed AG's five stated problems. Three real, one (413) misattributed to
   the wrong stage, one (verifier blocks on qualification) factually wrong about the code.
   Catalogued 24 further defects. All in the plan file.
2. **Research** — two Perplexity prompts run and verified.
3. **Runability slice** — provider primary `groq/compound` → `openai/gpt-oss-120b`; run-wide
   deadline + per-provider circuit breaker; header-driven 429 backoff; verifier evidence capped
   to supporting cards (22.5k → 2.8k chars, −87%); `attributed` branch added to `claim_strength`.
4. **Attribution + dead-weight slice** — see `reference/stress_findings.md` F1/F2/F3.
5. **Stress harness** — `scripts/stress_run.py`, paces against the 8K TPM bucket, logs structured
   results. It found F3 on its first run.

---

## 5. Corrections to prior documents — IMPORTANT

- **`HANDOFF.md:89` is STALE.** It says `gemini-2.5-flash` 404s / is retired. **Verified live
  2026-08-25: it returns 200.** So do `gemini-flash-latest` and `gemini-3.1-flash-lite`.
  The current `provider.py` fallback target is fine. I previously told the human to revert it
  and was wrong.
- **`HANDOFF.md:97` is CORRECT** — Gemini free tier really is ~20 requests/day/model. I briefly
  retracted this and was wrong to; the limit is not in the AI Studio table view, it is in the
  "Peak requests per day (RPD)" trend chart. **Gemini therefore cannot be primary** (~3 prospects/day).
- **Groq rate-limit table is authoritative** in `Groq API Key Rate Limit.md` at repo root
  (screenshot transcription, verified against live response headers).
- **`Gemini API Key Rate Limits.md`** at repo root is a `peak usage / limit` table, not a
  limits table. Both files belong in `reference/` — never moved, still at root.
- **Groq prompt caching is NOT observable on this account.** Two identical 1,076-token prompts
  returned identical `prompt_tokens` and no `prompt_tokens_details.cached_tokens`. Perplexity
  claimed cached tokens are TPM-exempt; unsupported here. Do not design around it.
- The 413 on `groq/compound` was **not** TPM overflow (compound has 70K TPM). It is agentic;
  server-side tool expansion overflows the model context. Compound is now unwired.

---

## 6. Budget and environment

- **Groq is the binding constraint: 8K TPM, 200K TPD, 1K RPD** on `openai/gpt-oss-120b`.
  ~~A prospect costs ~5.2K tokens → ~35 runs/day.~~ **CORRECTED 2026-08-25 by measurement**
  (run `222e2f57cdae`, first fully instrumented run): a prospect cost **13,311 tokens →
  ~15 runs/day**, i.e. **1.66x the per-minute bucket**, so every run stalled ~52s on a 429.
  57% of that run's 90.8s wall time was the rate-limit wait, not work.
  After batching the ranker into one call and capping its `reason` field at 15 words, a run
  measures ~8.1K on the ShipBob snapshot (~10.7K extrapolated live) → **~18 runs/day, still
  ~1.3x the bucket**. See `reference/stress_findings.md` F4. Do not remove `--gap` pacing.
- Tavily: 1,000 credits/month, ~90 used. Apify: `$0.296` of a `$4.00` cap. Exa: no local counter.
- **Always run python as `PYTHONPATH=. env -u GROQ_API_KEY ./venv/bin/python …`** — a stale
  7-char placeholder in the shell shadows the real key (it is currently absent, but the
  gotcha recurs).
- Test command is mandatory and exact:
  `env -i PATH=/usr/bin:/bin HOME=$HOME PYTHONPATH=. ./venv/bin/pytest tests/ -q`
- Prompt-keyed fixtures: changing a ranker/drafter/verifier/classifier prompt invalidates the
  hash. Confirm hash stability across two runs, then re-record with a live call.
- **No secrets appear in any file written this session.** Keys live only in `.env.local`
  (gitignored) and were never echoed. `reference/stress_log.jsonl` contains public
  professional data only (executive name, title, employer) — no contact details, by design.

---

## 7. Immediate next steps

1. **Offer to commit** the 15-file slice first, so each future stress finding lands on a clean base.
2. **Waiting on the human:** they need to paste `perplexity_prompts/04_stress_test_prospect_set.md`
   into Perplexity and save the reply to `perplexity_responses/04_stress_test_prospect_set.md`.
   It asks for ~40 real prospects across 10 adversarial categories (ambiguous company names,
   namesake-heavy people, executives with no web presence, non-ASCII names, renamed companies,
   out-of-ICP rows, …). Deliberately excludes email addresses — the pipeline never uses them.
3. When it lands: convert to `reference/stress_set.csv`
   (`category,name,title,company,domain`), then batch:
   `PYTHONPATH=. env -u GROQ_API_KEY ./venv/bin/python scripts/stress_run.py --limit 5`
4. Append every new defect to `reference/stress_findings.md` with root cause and fix.

**The human's stated goal: keep stress testing, find breakpoints, iterate, document as we learn,
make the product live.** Fix-then-rerun-the-same-prospect is the loop that worked today.

---

## 8. Known-open, not yet fixed

Full list is in the plan file. The ones that matter next:

- ~~**Verifier outcome is non-deterministic at `temperature=0`**~~ — **WITHDRAWN 2026-08-25.**
  The evidence was the two Versapay runs in `stress_log.jsonl`, which are 2 minutes apart and
  differ because the F3 fix landed *between* them (run 0 blocked on "intercompany reconciliation
  sprawl", which *is* F3). Nothing recorded which code produced each run, so a code change was
  read as model variance. Every run now carries a fingerprint (git SHA + working-tree digest +
  value_prop hash + model) and `runlog.py --diff` reports it first, precisely to stop this.
  A real determinism defect *did* exist upstream — card order was fetcher-completion order and
  the ranker's stable sorts carried it into the winning-card pick — now fixed with a
  deterministic tiebreak and covered by `tests/test_determinism.py`.
- **`ApifyLinkedInCompany` is ~16s of a ~35s run** — largest remaining latency. It earns its
  place (sole source of headcount → `icp_fit`) but belongs off the critical path.
- **No configured CTA / sender identity** (D18). Under retry pressure the drafter drops the call
  to action. Ruling #5: the sender profile must be an **in-app feature** (create/save/edit),
  not a config file. Fields: sender person, role, company, one-liner, outreach intent, CTA,
  tone, length.
- **Only one hook option is ever produced** (D15) — *measured 2 hooks on runs 0 and 1, so this
  may overstate the problem; re-check against live runs before acting.* — `tier ∈ {company, person}` collapses the
  Compass VI swap test to at most two survivors. Ties to open question Q3: split `tier` into
  `proximity` and `hook_kind`.
- **Pain list needs rebuilding** (ruling #7 fallout). `close_drag`, `silent_breaks`,
  `spreadsheet_exceptions` name job postings as their only `observable_via`; research 03
  independently judged two of four pains "not reliably observable" without them. Risk: the list
  collapses to `structural_complexity`, the only one that never needed job ads.
- **Settings UI ↔ engine schema drift** (D19) — `app.py` writes `icp.headcount.min/max`,
  `ranker.py` reads `preferred_min/preferred_max`; `yaml.dump` erases all comments in
  `value_prop.yaml`; `load_value_prop` is `lru_cache`d so saves need a restart.
- ~~**Early exit** still discards cards after the first ≥0.8 hook.~~ **FIXED 2026-08-25** —
  retired along with the ranker chunking; every capped card is now scored.

---

## 9. Suggested skills

Call these with the `Skill` tool when the matching work comes up:

- **`mattpocock-skills:diagnosing-bugs`** — for the verifier non-determinism in §8, and for any
  crash the stress batches surface. This is the most likely first call.
- **`mattpocock-skills:code-review`** — before committing the 15-file slice, review since
  `90aa2bf`. Two axes: repo standards (typed results over exceptions;
  `empty`/`failed`/`skipped` discipline) and spec (the 7 rulings in the plan file).
- **`mattpocock-skills:domain-modeling`** — when tackling Q3 (splitting `tier` into `proximity`
  and `hook_kind`). It is a vocabulary problem before it is a code problem, and an ADR is
  warranted since `tier` currently does three jobs.
- **`mattpocock-skills:tdd`** — for the sender-profile feature (D18). It is new surface with a
  clear contract, and the repo already has a fixture-replay test culture.

Do **not** invoke research/deep-research skills for provider or data-source questions — that
ground is covered in `perplexity_responses/01–03` and `reference/data-source-strategy.md`,
which `CLAUDE.md` explicitly says not to re-research.
